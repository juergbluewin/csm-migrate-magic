

services:
  cisco-migration-tool:
    build: .
    ports:
      - "3000:3000"
    
    environment:
      - NODE_ENV=production
      - CSM_BASEURL=https://10.240.74.156/nbi
      - CSM_VERIFY_TLS=false
    restart: unless-stopped